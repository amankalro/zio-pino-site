# Working agreement

## Command authorization

Claude has standing permission to run any command needed to carry out the
requested work in this repository — including installs, builds, git operations,
file edits, and running scripts — **without asking for confirmation first**.

### The one exception

**Never delete, move, or overwrite files or data located outside this project
folder** (`/Users/djangoveda/pino_site`). Deletions and destructive operations
are only permitted inside this folder. Anything that would remove or overwrite
data outside this folder must be confirmed with the user first.

Within this folder, deleting or replacing files that are part of the work is
fine.
